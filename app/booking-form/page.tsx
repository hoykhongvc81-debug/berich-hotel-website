import BookingForm from "@/components/BookingForm";

export const metadata = {
  title: "ระบบออกใบจอง — Berich Hotel",
  robots: { index: false, follow: false },
};

export default function BookingFormPage() {
  return <BookingForm />;
}
